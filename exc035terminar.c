/*Efetuar a leitura do nome do aluno, matrícula e 3 
notas. Informar se o aluno foi aprovado 
ou reprovado. Para notas >= 6 aprovado, caso 
contrário reprovado.*/
#include<stdio.h>

int main() {
    char nome[30]; float nota1, nota2, nota3;

    printf("Digite o nome do aluno: ");
    puts(nome);
    printf("Digite a primeira nota: ");
    scanf("%f", &nota1);
    printf("Digite a segunda nota: ");
    scanf("%f", &nota2);
    printf("Digite a terceira nota: ");
    scanf("%f", &nota3);

    printf("De acordo com as notas digitadas o aluno %s foi: ", nome);
    if (nota1 >)

}